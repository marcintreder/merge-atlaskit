import * as React from "react";
import Button from "../Button";

export default (
  <Button
    uxpId="button0"
    shouldFitContainer
    appearance="primary"
    isDisabled={false}
    isLoading={false}
  >
    Hello!
  </Button>
);
